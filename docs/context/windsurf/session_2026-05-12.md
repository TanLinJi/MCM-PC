# Session 2026-05-12

## 主题

用户要求回到顶会论文主线，详细解释 P1、anchor pollution、stable anchor source、conditional anchor switching，并强调：

- 需要定期保存对话记录，便于上下文超限后恢复。
- 英文术语和缩写第一次出现必须详细解释。
- 经过理论或实验验证的新想法，要先按顶会论文写法写进草稿，再安排验证。
- 需要整体架构图帮助理解 stable anchor source / stream anchor / abstention 在方法中的位置。
- 2026-05-13 用户进一步指出：strict TTA 不能假设测试样本干净，也不能默认使用带标签 source training anchors。后续术语改为 stable anchor source，并区分 clean oracle、source-available prototype、deployable text/vMF anchor。

## 本次关键结论

1. P1 不是失败；失败的是 D19 raw ICP-CD scale 修复路线。
2. P1 anchor pollution simulation 覆盖 ModelNet-C 7 个 corruption family × 5 个 severity，每个 setting 2468 个样本。
3. scale_2 上 feature 仍可靠：cos mean 0.9306，class-consistent 95.5%。
4. scale_2 上 anchor source 是决定性变量：corrupt anchor 84.44%，clean anchor 95.46%，提升 +11.02pp。
5. jitter_3 是相反机制：clean anchor 30.75%，corrupt anchor 81.93%，下降 -51.18pp。
6. C1 从 "geometry-as-feature-backup" 改为 "corruption-aware anchor source selection"。
7. stable anchor source 是主术语；clean anchor / source prototype 是诊断或上界，不是 strict source-free TTA 主方法。

## 已落盘动作

- `user_preferences.md` 增加定期保存上下文与详细术语解释规则。
- P1 复盘编号从冲突的 D20 改为 D22。
- `decisions.md` 增加 D22，D21 raw ICP-CD 超参收敛标为作废。
- `key_findings.md` 增加 F6/F7。
- `next_steps.md` 更新为 D22 conditional anchor switching 主线。
- `docs/paper/01_introduction.md` 把 C1 改写为 conditional anchor switching。
- 新增 HTML 架构图 `docs/reports/2026-05-12_conditional_anchor_switching_flow.html`。
- 新增 ModelNet-C 数据结构说明 `docs/reports/2026-05-12_modelnet_c_structure.html`。

## 下一步

1. 向用户解释 anchor pollution simulation、clean anchor、jitter feature failure、stable anchor source / stream anchor / abstention。
2. 实现前先让用户确认整体流程图是否符合直觉。
3. 若确认，进入 `model_with_conditional_anchor.py` 的最小 runner 设计。
