# Session 2026-05-13

## 主题

用户指出两件需要立即纠正的问题：

1. 之前没有逐条回答“anchor pollution 验证是否充分、相关实验结果在哪里”这个问题。
2. 新生成的 HTML 页面视觉质量不够好，并且应采用暗黑主题。

## 新增用户偏好

- 用户列出编号问题时，必须逐条详细回答，不能漏答。
- 如果某个问题需要实验或文件证据，必须给出证据路径、数据范围和结论强度。
- 给用户阅读的 HTML 默认采用暗黑主题，需要有更好的视觉层级和可读性。

已写入 `docs/context/windsurf/user_preferences.md` 第 15、16 条。

## 已完成修改

- `docs/reports/2026-05-12_modelnet_c_structure.html` 改为暗黑主题。
- `docs/reports/2026-05-12_conditional_anchor_switching_flow.html` 改为暗黑主题。
- 重新核对 P1 结果文件位置：
  - `docs/experiments/p1/P1_pollution_sim.md`
  - `docs/experiments/p1/P1_full_drift.md`
  - `Point-Cache/reports/P1_pollution_sim.json`
  - `Point-Cache/reports/P1_full_drift.json`

## 对 anchor pollution 证据的当前结论

- Anchor pollution simulation 覆盖 ModelNet-C 的 35 个 corrupted settings。
- 每个 setting 使用 2468 个样本。
- 结果主表在 `docs/experiments/p1/P1_pollution_sim.md` 第 7-43 行。
- Feature drift 主表在 `docs/experiments/p1/P1_full_drift.md` 第 7-44 行。
- 结论强度：足以强力支持 scale/rotate/add 等 affine-like corruption 的 anchor pollution 机制，但仍需要真实 end-to-end conditional anchor runner 验证可部署方法收益。
